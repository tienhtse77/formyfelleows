---
layout: page.njk
title: "All Posts"
description: "Complete archive of all blog posts on formyfelleows"
---

<div class="posts-archive">
    {% if collections.posts.length > 0 %}
        {% for post in collections.posts %}
            <article class="archive-post">
                <header class="archive-post-header">
                    <h3 class="archive-post-title">
                        <a href="{{ post.url }}">{{ post.data.title }}</a>
                    </h3>
                    <div class="archive-post-meta">
                        <time datetime="{{ post.date | date('Y-m-d') }}">
                            {{ post.date | dateFilter }}
                        </time>
                        <span class="reading-time">{{ post.templateContent | readingTime }}</span>
                    </div>
                </header>
                
                {% if post.data.description %}
                <p class="archive-post-excerpt">{{ post.data.description }}</p>
                {% endif %}
                
                {% if post.data.tags %}
                <div class="archive-post-tags">
                    {% for tag in post.data.tags %}
                        {% if tag != "post" %}
                        <span class="tag">#{{ tag }}</span>
                        {% endif %}
                    {% endfor %}
                </div>
                {% endif %}
            </article>
        {% endfor %}
    {% else %}
        <p>No posts yet. Check back soon!</p>
    {% endif %}
</div>